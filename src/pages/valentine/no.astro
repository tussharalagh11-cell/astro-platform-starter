---
const backLink = '/valentine';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>No Response</title>
  </head>
  <body>
    <main class="response no">
      <div class="card">
        <h1>Maybe another time 💔</h1>
        <p>
          No pressure, Divya — your happiness matters most, always.
        </p>
        <a class="button" href={backLink}>Back to the prompt</a>
      </div>
    </main>
  </body>
</html>

<style>
  :global(body) {
    margin: 0;
    font-family: 'SF Mono', 'Fira Code', ui-monospace, SFMono-Regular, Menlo, Monaco,
      Consolas, 'Liberation Mono', 'Courier New', monospace;
    background: radial-gradient(circle at top, #2a2f3a 0%, #11151f 55%, #050609 100%);
    color: #e7eaf0;
  }

  .response {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 32px 20px;
  }

  .card {
    max-width: 520px;
    padding: 32px 28px;
    background: rgba(9, 11, 17, 0.85);
    border-radius: 24px;
    text-align: center;
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.35);
    border: 1px solid rgba(255, 255, 255, 0.08);
  }

  h1 {
    margin: 0 0 12px;
    font-size: 2.2rem;
  }

  p {
    margin: 0 0 24px;
    line-height: 1.6;
  }

  .button {
    display: inline-block;
    text-decoration: none;
    padding: 12px 20px;
    border-radius: 999px;
    background: #f2f4f8;
    color: #0a0c11;
    font-weight: 600;
  }
</style>
